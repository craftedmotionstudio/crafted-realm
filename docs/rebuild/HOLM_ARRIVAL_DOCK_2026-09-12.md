# Working landing preview and warm-up ownership repair

Previous turn was progress: seven Blender asset types were added and the foreground boot failure was measured. The full tutorial island goal remains active. This pass extends the redesigned arrival slice over the water, rather than treating the house walk as the completed island.

`tools/blender/build_holm_arrival_dock_v1.py` produces the editable `.studio-workspaces/holm-arrival-dock-v1/candidates/dock.blend` and `dock.glb`, plus `dock.manifest.json`. Actual Blender export succeeded: 3020 triangles, seven authored meshes, ten exported primitives, five materials. The 3.4 by 9 tile deck has individually modeled planks, piles, under-deck braces, rails, rope and mooring horns. Above-deck obstacle bounds exclude the declared walking rectangle. Reference: Fishing_Pier_Option1 (reviewed by the asset author). Main reviews the placed result directly; no final visual approval is inferred from export checks.

The first seam test failed. The existing approach corridor ends at z118.58, while the first dock proposal began at119. Moving the actual authored north edge to118.5 (rather than inventing a navigation ramp) joins the landing stance on the existing flat Y1 shore. Final dock world centre is61.5,1,123; its far edge is127.5. The source declaration is `docs/rebuild/holm-overhaul/arrival-dock.json`.

`src/holm_arrival_dock.js` composes the existing approach/house facade with explicit dock surfaces. 171 stance nodes. Tests prove cardinal dock/shore/chart/upstairs return routes, fixed deck height, side/far-rail footprint limits, shore support under the entrance footprint, water rejection without the deck, raised-seam rejection, deterministic graphs and dock checkpoint roundtrip. This is source-envelope navigation, not full mesh/capsule acceptance or live ferry behavior.

Studio loads the actual Blender dock, raycasts named DockDeck meshes with rails occluding the deck, retains exterior house rendering from the dock, and can checkpoint the dock surface. Real in-app pointer click on the visible deck reached dock:60,125 atY1. Save and reload restored that exact dock stance. Selected the upstairs destination from there; final route evidence follows below. The preview currently retains its general open-house-doors restriction for walk commands; full door-state interaction is not complete.

Visual review: the dock gives the landing a clear purpose and a readable route across the shore edge. Width fits the player, planks/rails/piles separate clearly, and material colors fit the house/landscape. Remaining gaps: ferry/boat and its interaction, refined shore transitions and water motion, more natural vegetation silhouettes, a complete planted court and refined house exterior/interior. No 9/10 comparison gate or live Safe Publish applied.

In parallel, the warm-up lifecycle regression demonstrated that temporary shader templates remained attached to the live scene between compile calls. `src/world_v2_warmup.js` now attaches the group only during each synchronous compile and detaches it in finally, including compile failure. It retains shared resources and the base/extra-light/hidden-owner passes. All five lifecycle regression scenarios pass after failing before repair; existing World V2 contract tests pass. This corrects ownership, not proven historical12FPS causality. No performance budget was relaxed. Final foreground smoke and live scene inspection follow below.

Final in-app house26 route: restored dock:60,125 atY1, walked to upper:65,96 atY5.8, then back to dock:61,126 atY1. Console errors zero. Screenshot scratchpad/holm_guide_house_overhaul_v1/dock_house26_return.png. Main also inspected Bible_References/Complete/Fishing_Pier_Option1.jpg directly: broad boards/support rhythm readable; our dock still needs a connected boat/service and shoreline refinement.
Live warm-up inspection after repair: no retained world-v2-warmup scene group,28templates/base18programs, base compile1690.4ms, no warm-up errors. Immediate Play skipped deferredpasses as designed. Boot coordinator4131.8ms (shader1759.6ms/player1341ms); foreground smoke verdict pending. This one result does not prove a stable speed improvement.


Foreground smoke result: FAIL. Boot4977ms passed; structural105/105; walk7046ms; stream6boundaries/25tiles with exactsave/load passed; console errors0. Performance9FPS/worst173ms/125calls/34270triangles/3ticks fails unchanged budgets. Evidence dock26-smoke.json. Settled slow-frame profile (scratchpad/holm_perf/dock26-slow.cpuprofile):21381samples,20225idle (94.6%); largest namedJS selfsample114getObjectByProperty,102updateMatrixWorld. Renderer reports ANGLE NVIDIA RTX4070Laptop D3D11, visible document,1600x900 canvas. This points the next diagnosis toward GPU/presentation/rAF scheduling rather than assuming a busy JS loop; no causal conclusion or FPS fix established. Profiler disabled after capture.

