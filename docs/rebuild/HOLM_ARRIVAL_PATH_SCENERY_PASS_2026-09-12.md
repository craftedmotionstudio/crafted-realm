# Arrival path and scenery integration — 2026-09-12

The previous water pass was verified progress. This pass closes another visible Studio/runtime gap: the real arrival game now renders the designed footpath between the porch and landing. Shared src/holm_arrival_trail.js consumes the verified layout and uses the exact a,c,b / b,c,d terrain triangle interpolation. Studio uses the same builder with its coordinate offset. The path is a clickable exterior surface; existing navigation decides legal destinations. It adds one mesh/material and 1,270 triangles. Provider teardown removes registrations and disposes its geometry/material.

Pure tests use the actual authored layout/bundle and verify deterministic immutable geometry, Studio/world translation, contour/porch clipping, index bounds, all vertex and triangle-centroid height offsets, saddle-cell diagonal interpolation, and invalid inputs. There are 3,514 vertices and no degenerate triangles; sampled maximum interior penetration was below floating-point epsilon.

In-app real-input proof: Continue restored ground:66,97. Click on the visible trail walked through the open house door to exterior:65,112 at [65.5,1.767857,112.5]. Dock click continued to dock:61,124 at [61.5,1,124.5]. Return path click reached exterior:60,115 at [60.5,1.3125,115.5]. About 60 FPS, zero errors. Evidence scratchpad/holm_perf/arrival-trail-dock.png. This does not prove northern routes or complete-island traversal.

Default foreground regression smoke arrival-trail-gate-20260912 passed: 105/105, real out/back 3460ms, six boundaries and exact lossless save/load, 60 FPS, worst21ms, 92calls/33,044triangles, seven ticks, zero errors. Studio remained paused. Default-provider smoke and isolated-arrival pointer evidence are separate scopes.

Visual review against Landscape_Option.jpg: the path clearly connects services and reads from the gameplay camera; its warm earth separates from green ground. Grade changes remain visible as flat-shaded facets, with no detected geometry penetration. Unlike the reference, the playable landscape still lacks the grouped trees, planted banks, fence enclosure and shoreline rocks. The ocean is too bright/patterned in the game lighting for final acceptance. House and landscape art remain below the final bar. No score or full-art acceptance upgraded.

Prepared a pure scenery compiler in src/holm_arrival_landscape_contract.js with 35/35 headless checks and 4/4 boot checks. It returns transforms, grounding evidence, conservative rotated bounds and blocker candidates, rejects route/building overlap and excessive structural slope, and does not install anything. Synthetic placement tests are explicitly not actual model-fit proof. Actual Blender measurement is the next evidence step before package integration.

Blender evidence completed: tools/blender/measure_holm_arrival_landscape.py imports all seven actual GLBs in Blender4.5.10 LTS and evaluates transformed vertices. Breeze actions/slots explicitly activated; oak/hazel motion variation asserted over242 sampled key/midpoint/endpoint times each. Triangle totals1288/1498/204/532/440/340/1092 match manifests. The measured sampled animation unions compiled against all17 actual authored placements and terrain successfully, returning17 blockers. Outputs in .studio-workspaces/holm-arrival-landscape-measure-v1/candidates/measured.json and placement-contract.json. Samples are not a continuous swept-volume guarantee. No scenery installed yet; next step is package-owned verified scenery assets and the matching navigation envelopes.
